source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in dhl_express.gemspec
gemspec

group :development, :test do
  gem "bundler"
  gem "dotenv"
  gem "pry"
  gem "rake"
  gem "rspec"
  gem "rubocop"
  gem "vcr"
  gem "webmock"
end
